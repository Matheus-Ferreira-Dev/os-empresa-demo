# Aceitação: entregas

- `crie um carrossel` gera roteiro, legenda e checklist em `entregas/carrosseis/`.
- `crie uma landing page` gera briefing, copy, checklist e `index.html` em `entregas/paginas/`.
- Uma nova execução cria `v2` e preserva `v1`.
